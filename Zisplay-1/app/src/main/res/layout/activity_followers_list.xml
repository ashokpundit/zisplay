<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="vun.zisplaymerchant.activity.FollowersList">

    <TextView android:text="Followers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/heading"
        android:layout_margin="5dp"
        />

    <Button android:text="Invite Followers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/inviteFollowers"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/heading"
        android:layout_margin="5dp"
        android:onClick="inviteFollowers"
        />

    <Button android:text="Search Followers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/searchFollower"
        android:layout_alignRight="@+id/inviteFollowers"
        android:layout_below="@+id/heading"
        android:layout_alignParentRight="true"
        android:layout_margin="5dp"
        />

    <ListView android:id="@+id/lst_followersList"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/inviteFollowers"
        >

    </ListView>

</RelativeLayout>
